# Procedural Sprites

Gameplay visuals rely on procedural sprite descriptions that are generated at runtime by the rendering pipeline.
These definitions receive transformation and tint parameters so that shared drawing logic can be reused across effects.

Legacy placeholder files that once occupied explicit asset directories have been retired now that sprites are authored entirely in code.
