# boss_manager.md

This note retains a sanitized description of the **BossManager** while withholding privileged combat orchestration details.

## Overview
- `BossManager` coordinates boss lifecycle events and alignment-aware scaling.
- `[REDACTED]` hides the precise intro scripting, defeat handling edge cases, and escalation routines.

## Sanitized Responsibilities
- Spawn, update, and removal flows remain under `BossManager`, ensuring encounters progress through registered phases.
- Notifications to stage systems continue, but timing thresholds, fail-safe triggers, and recovery tactics are `[REDACTED]` and stored only in the private backup.
