# Assets Overview

This folder outlines the external assets bundled with Purge of the Crescent Veil.

## Audio Files

Sound effects are generated in real time by the in-engine synthesizer.
Looping `.ogg` tracks are included as a fallback for ambient music.
If runtime synthesis is unavailable, the system logs a warning and skips playback.
