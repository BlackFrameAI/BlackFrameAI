# Purge Crescent Codex Universal Container Seed v2 — Final Compatibility Build Chain

sudo apt-get update -y

# Base compiler + build tools
sudo apt-get install -y build-essential cmake ninja-build pkg-config

# Graphics stack: OpenGL, GLFW, X11, Wayland (even if disabled, satisfies CMake)
sudo apt-get install -y libgl1-mesa-dev libglu1-mesa-dev libglfw3-dev \
libxrandr-dev libxi-dev libxinerama-dev libxcursor-dev libxkbcommon-dev \
libwayland-dev wayland-protocols wayland-scanner++ wayland-scanner \
libwayland-egl-dev libwayland-server-dev libwayland-client-dev

# Audio stack
sudo apt-get install -y libopenal-dev libpulse-dev libasound2-dev

# Lua (stable 5.4 bindings)
sudo apt-get install -y lua5.4 liblua5.4-dev

# JSON libraries (nlohmann)
sudo apt-get install -y nlohmann-json3-dev

# Extra dev tools for Codex support
sudo apt-get install -y unzip curl git python3 python3-pip python-is-python3

# Cleanup (optional)
sudo apt-get autoremove -y
