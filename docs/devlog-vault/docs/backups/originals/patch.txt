diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1f38ab34ca9d7565a0f0be9a04d22dded941ed76..17821879dae53db252d792f582bc023048146a92 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -489,51 +489,50 @@ target_compile_options(RuntimeLauncher PRIVATE ${CV_WARNING_FLAGS} ${CV_WERROR_F
 target_include_directories(RuntimeLauncher PRIVATE
     ${PROJECT_SOURCE_DIR}/engine
 )
 set_target_properties(RuntimeLauncher PROPERTIES UNITY_BUILD ${CV_USE_UNITY_BUILD})
 ## RuntimeLauncher compiles without a precompiled header
 # to keep dependencies minimal
 cv_apply_win_defs(RuntimeLauncher)
 
 # CollapseShapeViewer dumps CollapseLineageLogger data
 add_executable(CollapseShapeViewer EXCLUDE_FROM_ALL
     tools/CollapseShapeViewer/main.cpp
 )
 target_link_libraries(CollapseShapeViewer PRIVATE cv_engine)
 target_compile_options(CollapseShapeViewer PRIVATE ${CV_WARNING_FLAGS} ${CV_WERROR_FLAG})
 target_include_directories(CollapseShapeViewer PRIVATE
     ${PROJECT_SOURCE_DIR}/engine
 )
 set_target_properties(CollapseShapeViewer PROPERTIES UNITY_BUILD ${CV_USE_UNITY_BUILD})
 target_precompile_headers(CollapseShapeViewer REUSE_FROM cv_engine)
 cv_apply_win_defs(CollapseShapeViewer)
 
 # Crescent runtime executable
 add_executable(crescent_runtime
     engine/core/engine/main.cpp
     engine/core/engine/Engine.cpp
-    game/modules/minimal/MinimalGame.cpp
     game/core/Game.cpp
     game/core/EntityManager.cpp
     game/modules/player/PlayerManager.cpp
     game/modules/player/PlayerController.cpp
     game/modules/system/projectile/ProjectileManager.cpp
     game/modules/enemy/EnemyManager.cpp
     game/modules/enemy/EnemyAIController.cpp
     game/modules/enemy/EnemySpawnHandler.cpp
     game/modules/enemy/EnemyCollisionRegistrar.cpp
     game/modules/enemy/behaviors/DefaultEnemyBehavior.cpp
     game/modules/enemy/behaviors/ScriptedEnemyBehavior.cpp
     game/modules/enemy/spawner/EnemySpawner.cpp
     game/modules/stage/StageProgression.cpp
     game/modules/stage/SpawnController.cpp
     game/modules/system/collision/CollisionSystem.cpp
     game/modules/system/powerup/PowerupManager.cpp
     game/modules/system/particle/ParticleEffect.cpp
     game/modules/graphics/SpriteRegistrations.cpp
     game/modules/system/particle/ParticleManager.cpp
     game/modules/enemy/types/boss/Boss.cpp
     game/modules/enemy/types/boss/SampleBoss.cpp
     game/modules/enemy/types/boss/BossManager.cpp
     game/modules/debug/DebugController.cpp
     game/modules/stage/StageManager.cpp
     game/modules/system/alignment/AlignmentManager.cpp
diff --git a/docs/devlogs/engine_devlog.md b/docs/devlogs/engine_devlog.md
index bda88769f61517b4719d4aa0e18dcaf9ca171175..1551ee87c4c5c5f985bbe7ad5ba4b2b5005fd66a 100644
--- a/docs/devlogs/engine_devlog.md
+++ b/docs/devlogs/engine_devlog.md
@@ -1784,25 +1784,27 @@ Chronological notes on engine updates.
 ## 2025-07-18T17:16:12Z
 - StencilGuideSystem now guards descriptor emission with an internal `m_emitting` flag to avoid recursive callbacks.
 ## 2025-07-18T17:26:09+00:00
 - Descriptor callbacks now return a handle for removal. ProceduralUIGenerator stores this handle and unregisters on destruction.
 
 ## 2025-07-18T19:13:50Z
 - LocalizationSystem now loads tables from `game/assets/localization`.
 - VectorFont supports all printable ASCII characters and converts lowercase input automatically.
 ## 2025-07-18T19:48:59Z
 - ProceduralSpriteManager now validates RenderSystem initialization and sprite resources before drawing. Missing resources emit a single warning and drawing is skipped.
 ## 2025-07-18T20:07:05+00:00
 - Engine initializes runtime directories (`logs` and `game/saves`) automatically.
 
 ## 2025-07-18T20:21:34+00:00
 - Removed StbFontRenderer module and all related scene code. VectorFont now
   handles all text rendering.
 ## 2025-07-18T21:35:42Z
 - ProceduralSpriteInstance now includes an `enableDrawLogs` flag for optional draw logging.
 ## 2025-07-18T21:53:25+00:00
 - DualLayerQuantumManager now waits up to 1s for ChaoticCollapseOracle to become ready and logs a warning when timeout triggers.
 ## 2025-07-18T22:52:50+00:00
 - ProceduralSpriteInstance `render()` now checks `shouldDraw()` and only logs draw state when `enableDrawLogs` is true.
 - ProceduralSpriteManager tracks drawn sprite count and logs it when `RenderSystem::isLogSpriteCounts()` is enabled.
 ## 2025-07-18T23:08:44+00:00
 - ProceduralSpriteInstance logs sprite name before drawing and appends draw context to post-draw logs.
+## 2025-07-18T23:35:21+00:00
+- Removed MinimalGame as a test harness. The real Game class is now always used for engine initialization.
diff --git a/docs/modular/game_system_tree.md b/docs/modular/game_system_tree.md
index d178ca40f0e4911c9c829c44612f160d192c684c..dec95f070590852f54a0c864d50add59e9710b67 100644
--- a/docs/modular/game_system_tree.md
+++ b/docs/modular/game_system_tree.md
@@ -63,64 +63,63 @@ This tree is the authoritative reference for which systems have been modularized
 - ✅ **CollapseLineageLogger** (`engine/modules/quantum/logging/CollapseLineageLogger.*`) – Completed
   - records entropy hash and qubit states on collapse
 - ✅ **cuQuantumCollapseSim** (`engine/modules/quantum/statevector/cuQuantumCollapseSim.*`) – Completed
   - records probability cache statistics (see `../systems/quantum_statevector_system.md#probability-cache-usage`)
 - 🟡 **CudaStateVectorSimulator** (`engine/modules/quantum/cuda/CudaStateVectorSimulator.*`) – In-Progress
   - offers raw CUDA acceleration when enabled
 - 🟡 **HybridGatedCollapseSimulator** (`engine/modules/quantum/collapse/HybridGatedCollapseSimulator.*`) – In-Progress
   - gate-controlled collapses with EntropyManager seeds
   - watchdog resets require `requestWatchdogReset()`
 - ✅ **ChaoticCollapseOracle** (`engine/modules/quantum/collapse/ChaoticCollapseOracle.*`) – Completed
   - asynchronous entropy collapse pool
   - seed validation and observer logging
   - chaos-aligned retrieval interface via `ChaoticCollapsePool`
 - 🟡 **DualLayerQuantumManager** (`engine/modules/quantum/DualLayerQuantumManager.*`) – In-Progress
   - ✅ **ChaoticSeedPromoter** (`engine/modules/quantum/collapse/ChaoticSeedPromoter.*`) – Completed
   - probability-based replacement of gated collapse seeds
  - ✅ **CollapseStateMapper** (`engine/modules/quantum/qptl/CollapseStateMapper.*`) – Completed
   - validates and promotes seeds per `IntentRequest`
  - ✅ **QuantumPatternTranslationLayer** (`engine/modules/quantum/qptl/QuantumPatternTranslationLayer.*`) – Completed
 - ✅ **GateRegistry** (`engine/modules/quantum/gates/GateRegistry.*`) – Completed
 - 🟡 **CollapseWatchdog** (`engine/modules/quantum/CollapseWatchdog.*`) – In-Progress
 - ✅ **CombatSimulator** (`modules/combat/simulator/CombatSimulator.*`) – Completed
   - uses `CombatScenario`
 - ✅ **DebugController** (`modules/debug/DebugController.*`) – Completed
 - ✅ **Game** (`core/Game.*`) – Completed
-- ✅ **MinimalGame** (`modules/minimal/MinimalGame.*`) – Completed
 - ✅ **DialogueTree** (`modules/system/narrative/DialogueTree.*`) – Completed
  - ✅ **Boss classes** (`modules/enemy/types/boss/Boss.*`, `modules/enemy/types/boss/SampleBoss.*`) – Completed
 - ✅ **EnemyStateMachine** (`modules/enemy/EnemyStateMachine.h`) – Completed
 - ✅ **ProceduralSprite registry** (`modules/graphics/SpriteRegistrations.*` and `modules/graphics/*Visuals.h`)
 
 ## Runtime Scenes
 - ✅ **Scene0** (`scene/Scene0.*`) – Completed
 - `scene/Scene1.*`
 - `scene/Scene2.*`
 - `scene/Scene3.*`
 
 ## Core Headers
-- `core/IGame.h` – interface implemented by `Game` and `MinimalGame`
+- `core/IGame.h` – interface implemented by `Game`
 - `core/EntityManager.h` – basic entity tracking
 - `modules/enemy/Enemy.h` – enemy data structure
  - `modules/system/powerup/Powerup.h` – powerup data structure
  - `modules/system/particle/ParticleEffect.h` – particle effect definition
 - `core/include/pch.h` – precompiled header for game sources
 - `engine/core/scene/IScene.h` – base scene interface
 - `engine/core/scene/Scene.h` – list of drawables used by RenderSystem
 - `engine/core/scene/Camera.h` – 2D camera with shake and zoom
 
 ## Unclassified or Helper Systems
  - ✅ **PassiveTree** (`modules/system/chapter/PassiveTree.*`)
 - ✅ **PassivesSystem** (`modules/system/chapter/PassivesSystem.*`) – Completed
 - ✅ **CombatScenario** (`modules/combat/CombatScenario.h`) – Completed
  - ✅ **ProceduralSprites** (`modules/graphics/ProceduralSprites.h`) – Completed
  - ✅ **EffectSprites** (`modules/graphics/EffectSprites.h`) – Completed
  - ✅ **FXHelpers** (`modules/graphics/FXHelpers.h`) – Completed
  - ✅ **VectorFont** (`modules/vectorfont/VectorFont.h`) – Completed
    - 🟡 **ProceduralFontGenerator** (`modules/font/ProceduralFontGenerator.*`) – In Progress
  - ✅ **ProceduralUI** (`modules/ui/ProceduralUI.h`)
  - 🟡 **ProceduralUIGenerator** (`ui/ProceduralUIGenerator.*`) – In Progress
  - ✅ **ProceduralSpriteRegistry** (`modules/graphics/ProceduralSpriteRegistry.*`)
 - ✅ **ProceduralSprite** (`modules/graphics/ProceduralSprite.*`)
 - 🟡 **ProceduralSpriteManager** (`engine/modules/procedural_sprite/ProceduralSpriteManager.*`) – In Progress
 ## Engine-Level Game Systems
 - ✅ **GameManager** (`game/core/flow/GameManager.*`) – Completed
diff --git a/docs/systems/game_flow_management.md b/docs/systems/game_flow_management.md
index aef892fc888a70a03d8e3661775293341da3801f..603559eed9440dac1dfe9fac039bf22e3ddc97be 100644
--- a/docs/systems/game_flow_management.md
+++ b/docs/systems/game_flow_management.md
@@ -1,17 +1,15 @@
 # game_flow_management.md
 
 The **game flow management stack** coordinates high level state transitions. These modules live under `game/core/flow/`.
 
 ## Components
 - **GameManager** – primary facade controlling scene transitions and delegating to other flow modules.
 - **SceneCoordinator** – wraps `SceneManager` to handle transitions and per-frame updates.
 - **PhaseController** – thin wrapper exposing `GamePhaseManager` APIs.
 - **GamePhaseManager** – tracks the current `GameState` such as `MainMenu`, `Playing` or `GameOver`.
 - **SaveLoadCoordinator** – centralizes save system access, collects manager state
   for serialization and stores pending profile info.
 
 Together they drive the engine from startup to shutdown. `GameManager::SetState` triggers scene transitions and updates the active phase. Loading and saving pass through `SaveLoadCoordinator`.
 
-When **minimal mode** is active, switching to the `Playing` state only calls `m_game->StartGame()` and keeps the current scene instead of reloading `Scene1`.
-
 See [docs/modular/game_system_tree.md](../modular/game_system_tree.md) for status tracking of these systems.
diff --git a/docs/systems/minimal_game.md b/docs/systems/minimal_game.md
deleted file mode 100644
index c084d8d158d4536e66f12875c1acaace8cc7037b..0000000000000000000000000000000000000000
--- a/docs/systems/minimal_game.md
+++ /dev/null
@@ -1,5 +0,0 @@
-# minimal_game.md
-
-`MinimalGame` is a stripped-down implementation of `IGame` used for engine samples and unit tests. It exposes only an empty `Scene` and an `EntityManager` so demonstrations can run without the full game stack.
-
-See [docs/modular/game_system_tree.md](../modular/game_system_tree.md) for the current modularization status.
diff --git a/engine/core/engine/Engine.cpp b/engine/core/engine/Engine.cpp
index 807f2aea23a8b7acbdb0685348362138f59b1977..29a7aae4adfe80175a573107d3bccccc49157a4f 100644
--- a/engine/core/engine/Engine.cpp
+++ b/engine/core/engine/Engine.cpp
@@ -111,64 +111,63 @@ readDisplayConfig(const std::string &path, bool &forceSingle) {
       p.width = wj.value("width", 1280);
       p.height = wj.value("height", 720);
       p.title = wj.value("title", i == 1 ? "Debug" : "Crescent Veil");
       p.backend =
           parseBackend(wj.value("backend", std::string{}), defaultBackend);
       params.push_back(p);
     }
   }
   if (params.empty()) {
     cv::DisplayParams p;
     p.backend = defaultBackend;
     params.push_back(p);
   }
   if (forceSingle && params.size() > 1) {
     params.resize(1);
   }
   return params;
 }
 } // namespace
 
 namespace cv {
 
 void Engine::Init(game::IGame *game, const std::string &language,
                   BackendType backend, const std::string &displayConfig,
                   bool enableCloudSave, bool invertWindowRoles, bool autotest,
-                  float autotestDuration, bool minimalMode,
+                  float autotestDuration,
                   const std::string &initialTheme) {
   namespace fs = std::filesystem;
   fs::path root = FileUtils::getProjectRoot();
   FileUtils::createDirectories(root / "logs");
   FileUtils::createDirectories(root / "game" / "saves");
   FileUtils::createDirectories(root / "game" / "saves" / "cloud");
   FileUtils::createDirectories(root / "game" / "saves" / "backups");
   // Initialize logging before any other subsystem to capture early messages.
   if (!Logger::initialize()) {
     Logger::fatal("Engine: Failed to initialize logger", __FILE__, __LINE__);
   }
   CV_LOG_INFO("Engine: Init");
-  m_MinimalMode = minimalMode;
   m_Autotest = autotest;
   m_AutotestTimer = autotestDuration;
 
   if (Diagnostics::isSafetyEnabled()) {
     Diagnostics::installSignalHandlers();
     Diagnostics::installStackOverflowHandler();
   }
   m_InvertWindowRoles = invertWindowRoles;
   bool forceSingle = false;
   std::vector<DisplayParams> displays =
       readDisplayConfig(displayConfig, forceSingle);
   m_displayManager = std::make_unique<MultiDisplayManager>();
   bool displayOk = m_displayManager->initialize(displays);
   if (!displayOk) {
     CV_LOG_FATAL("Engine: Failed to initialize MultiDisplayManager");
   }
 
   m_InputBroker = std::make_unique<engine::InputBroker>();
   m_InputBroker->Initialize(m_displayManager->getWindow(0));
   for (size_t i = 1; i < m_displayManager->getWindowCount(); ++i) {
     m_InputBroker->AddWindow(m_displayManager->getWindow(i));
   }
   m_InputManager = std::make_unique<engine::InputManager>(m_InputBroker.get());
 
   m_TimeManager = std::make_unique<engine::TimerManager>();
@@ -277,51 +276,51 @@ void Engine::Init(game::IGame *game, const std::string &language,
           CV_LOG_FATAL("Engine: OpenGL 4.5 or higher is required");
         }
       }
     } else {
       CV_LOG_FATAL("Engine: Unable to query OpenGL version");
     }
   }
   if (!SystemInfo::initialize()) {
     CV_LOG_FATAL("Engine: Failed to initialize SystemInfo");
   }
   [[maybe_unused]] const auto &snap = SystemInfo::getSnapshot();
   CV_LOG_INFO("Engine: System CPU " + snap.cpuBrand + ", Threads " +
               std::to_string(snap.threadCount) + ", RAM " +
               std::to_string(snap.totalRamMB) + " MB");
   GLFWwindow *mainWindow = m_displayManager->getWindow(0);
   auto *ctx = static_cast<WindowContext *>(glfwGetWindowUserPointer(mainWindow));
   if (!ctx) {
     ctx = new WindowContext();
     glfwSetWindowUserPointer(mainWindow, ctx);
   }
   ctx->render = m_RenderSystem.get();
   glfwSetFramebufferSizeCallback(mainWindow, GLFWFramebufferSizeCallback);
 
   m_GameManager = std::make_unique<engine::GameManager>(
       game, m_InputManager.get(), m_RenderSystem.get(), m_SaveSystem.get(),
-      m_LocalizationSystem.get(), m_MinimalMode);
+      m_LocalizationSystem.get());
   if (m_SaveSystem) {
     m_SaveSystem->setCheckpointCallback([this]() {
       if (m_GameManager &&
           m_GameManager->GetState() == engine::GameState::Playing) {
         m_GameManager->SaveGame("checkpoint", "Checkpoint");
       }
     });
   }
 
   // Initialize vector font subsystem before any scenes are created
   cv::VectorFont::Init();
   auto scene = std::make_unique<game::Scene0>(m_GameManager.get(),
                                              m_InputManager.get(),
                                              m_RenderSystem.get());
   m_GameManager->GetSceneManager().PushScene(std::move(scene));
 
   if (m_displayManager && m_displayManager->getWindowCount() > 1) {
     auto *binding = m_displayManager->getBinding(1);
     CV_LOG_INFO(std::string("Engine: debug binding=") +
                 std::to_string(reinterpret_cast<uintptr_t>(binding)) +
                 " index=1");
     if (binding) {
       CV_LOG_INFO("Engine: creating DebugOverlayWindow for index 1");
       CreateDebugOverlayWindow(binding, 1);
       m_debugScenes.push_back(std::make_unique<game::DebugWindow0>(
diff --git a/engine/core/engine/Engine.h b/engine/core/engine/Engine.h
index ff7180ba70ef9633863c8c3e01494fa3cbd831f7..1aeaacff78ffa1f593715f66195a0ad746cefe22 100644
--- a/engine/core/engine/Engine.h
+++ b/engine/core/engine/Engine.h
@@ -34,51 +34,50 @@ class LocalizationSystem;
 class TelemetryManager;
 class NetworkSimulationManager;
 class HILInterface;
 class DebugOverlayWindow;
 class ThemeManager;
 }
 
 namespace game {
 class IGame;
 class DebugWindow0;
 }
 
 namespace cv {
 
 // Engine class provides basic initialization, runtime loop, and shutdown.
 class Engine {
 public:
     // Initialize engine resources with a provided game implementation.
     void Init(game::IGame* game, const std::string& language = "en",
               BackendType backend = BackendType::OpenGL,
               const std::string& displayConfig = "game/assets/config/display.json",
               bool enableCloudSave = false,
               bool invertWindowRoles = false,
               bool autotest = false,
               float autotestDuration = 60.0f,
-              bool minimalMode = false,
               const std::string& initialTheme = "");
 
     ~Engine();
 
     // Run the main engine loop.
     void Run();
 
     // Access the primary RenderSystem.
     RenderSystem* GetRenderSystem() const { return m_RenderSystem.get(); }
     ThemeManager* GetThemeManager() const { return m_ThemeManager.get(); }
 
     // Access display windows and bindings
     GLFWwindow* GetWindow(size_t index) const;
     RendererBinding* GetBinding(size_t index) const;
     size_t GetWindowCount() const;
 
     // Optional debug overlay management
     DebugOverlayWindow* CreateDebugOverlayWindow(RendererBinding* binding,
                                                  size_t windowIndex);
     void SetDebugOverlayScene(size_t windowIndex, engine::IScene* scene);
 
     // Change the active visual theme at runtime.
     void SetTheme(const std::string& theme);
 
     // Shutdown and cleanup resources.
@@ -94,31 +93,30 @@ private:
     std::unique_ptr<MultiDisplayManager> m_displayManager;
     std::unique_ptr<engine::InputManager> m_InputManager;
     std::unique_ptr<engine::TimerManager> m_TimeManager;
     std::unique_ptr<engine::SynthEngine> m_SynthEngine;
     std::unique_ptr<RenderSystem> m_RenderSystem;
     std::unique_ptr<PostProcessingManager> m_PostProcessingManager;
     std::unique_ptr<ResourceSystem> m_ResourceSystem;
     std::unique_ptr<DynamicsSystem> m_PhysicsSystem;
     std::unique_ptr<ScriptingSystem> m_ScriptingSystem;
     std::unique_ptr<SaveSystem> m_SaveSystem;
     std::unique_ptr<LocalizationSystem> m_LocalizationSystem;
     std::unique_ptr<ThemeManager> m_ThemeManager;
     std::unique_ptr<TelemetryManager> m_TelemetryManager;
     std::shared_ptr<NetworkSimulationManager> m_NetworkManager;
     std::unique_ptr<HILInterface> m_HILInterface;
     std::unique_ptr<engine::GameManager> m_GameManager;
     std::unique_ptr<FrameTimer> m_FrameTimer;
     std::unique_ptr<FrameLimiter> m_FrameLimiter;
     std::unique_ptr<TimeSystem> m_TimeSystem;
     // TODO: Support multiple debug overlay windows.
     // Only index 1 is actively used, but the vector allows expansion.
     std::vector<std::unique_ptr<DebugOverlayWindow>> m_DebugOverlayWindows;
     std::vector<std::unique_ptr<engine::IScene>> m_debugScenes;
     game::IGame* m_Game = nullptr;
     bool m_InvertWindowRoles = false;
-    bool m_MinimalMode = false;
 };
 
 } // namespace cv
 
 #endif // ENGINE_H
diff --git a/engine/core/engine/main.cpp b/engine/core/engine/main.cpp
index b418ce8400976579e1c884415c3da78fbf606818..8d58c6a2381dd1f3ca7299ffbc6b77b3b109cf52 100644
--- a/engine/core/engine/main.cpp
+++ b/engine/core/engine/main.cpp
@@ -1,121 +1,111 @@
 #include <iostream>
 #include <cstring>
 #include <string>
 #include <cstdlib>
 #include "core/engine/Engine.h"
-#include "modules/minimal/MinimalGame.h"
 #include "game/core/Game.h"
 #include "modules/debug/logging/logger.h"
 #include "modules/debug/diagnostics/diagnostics.h"
 #include "seed/core/SeedCore.h"
 #include "seed/managers/SeedControl.h"
 #include "engine/modules/render/RenderSystem.h"
 #include "render/interfaces/RenderBackend.h"
 
 int main(int argc, char* argv[]) {
     std::string language = "en";
     cv::BackendType backend = cv::BackendType::OpenGL;
     std::string kernelPath;
     std::string updateRequest;
     std::string displayConfig = "game/assets/config/display.json";
     bool cloudSave = false;
     bool logOverlayPositions = false;
     bool invertWindowRoles = false;
     bool debugLogs = false;
     bool autotest = false;
     float autotestDuration = 60.0f;
-    bool minimalMode = false;
     std::string initialTheme;
     for (int i = 1; i < argc; ++i) {
         if (std::strcmp(argv[i], "--nosafety") == 0) {
             cv::Diagnostics::enableSafety(false);
         } else if (std::strncmp(argv[i], "--lang=", 7) == 0) {
             language = argv[i] + 7;
         } else if (std::strcmp(argv[i], "--vulkan1") == 0) {
             backend = cv::BackendType::Vulkan1;
         } else if (std::strcmp(argv[i], "--vulkan") == 0) {
             backend = cv::BackendType::Vulkan2;
         } else if (std::strcmp(argv[i], "--vulkan-rtx") == 0) {
             backend = cv::BackendType::VulkanRTX;
         } else if (std::strcmp(argv[i], "--dx11") == 0) {
             backend = cv::BackendType::DirectX11;
         } else if (std::strcmp(argv[i], "--dx12") == 0) {
             backend = cv::BackendType::DirectX12;
         } else if (std::strcmp(argv[i], "--dx12-rtx") == 0) {
             backend = cv::BackendType::DirectX12RTX;
         } else if (std::strcmp(argv[i], "--metal") == 0) {
             backend = cv::BackendType::Metal;
         } else if (std::strcmp(argv[i], "--android-gles") == 0) {
             backend = cv::BackendType::AndroidGLES;
         } else if (std::strncmp(argv[i], "--kernel=", 9) == 0) {
             kernelPath = argv[i] + 9;
         } else if (std::strncmp(argv[i], "--update-kernel=", 16) == 0) {
             updateRequest = argv[i] + 16;
         } else if (std::strncmp(argv[i], "--display-config=", 17) == 0) {
             displayConfig = argv[i] + 17;
         } else if (std::strcmp(argv[i], "--cloud-save") == 0) {
             cloudSave = true;
         } else if (std::strncmp(argv[i], "--cloud-save=", 13) == 0) {
             const char* val = argv[i] + 13;
             cloudSave = (std::strcmp(val, "1") == 0 || std::strcmp(val, "true") == 0);
         } else if (std::strcmp(argv[i], "--log-overlay-positions") == 0) {
             logOverlayPositions = true;
         } else if (std::strcmp(argv[i], "--invert-window-roles") == 0) {
             invertWindowRoles = true;
         } else if (std::strcmp(argv[i], "--debug") == 0) {
             debugLogs = true;
         } else if (std::strcmp(argv[i], "--autotest") == 0) {
             autotest = true;
         } else if (std::strncmp(argv[i], "--autotest=", 11) == 0) {
             autotest = true;
             autotestDuration = std::atof(argv[i] + 11);
-        } else if (std::strcmp(argv[i], "--minimal") == 0) {
-            minimalMode = true;
         } else if (std::strncmp(argv[i], "--theme=", 8) == 0) {
             initialTheme = argv[i] + 8;
         }
     }
 
     if (initialTheme.empty()) {
         const char* envTheme = std::getenv("CV_THEME");
         if (envTheme) initialTheme = envTheme;
     }
 
     if (debugLogs) {
         cv::Logger::setLogLevel(cv::Logger::Level::Debug);
     }
     cv::Engine engine;
-    std::unique_ptr<game::IGame> game;
-    if (minimalMode) {
-        game = std::make_unique<game::MinimalGame>();
-    } else {
-        game = std::make_unique<game::Game>();
-    }
+    std::unique_ptr<game::IGame> game = std::make_unique<game::Game>();
     engine::SeedControl seedControl;
     engine::SeedCore seed(seedControl);
     if (!kernelPath.empty()) {
         seed.Startup(kernelPath);
     }
     try {
         engine.Init(game.get(), language, backend,
                     displayConfig,
                     cloudSave, invertWindowRoles,
                     autotest, autotestDuration,
-                    minimalMode,
                     initialTheme);
         (void)logOverlayPositions;
         engine.Run();
     } catch (const std::exception& e) {
         cv::Diagnostics::logException(e, __FILE__, __LINE__);
         cv::Diagnostics::dumpEngineState();
     }
     engine.Shutdown();
     if (!updateRequest.empty()) {
         seedControl.RequestUpgrade(updateRequest);
     }
     seed.ProcessRequests();
     if (cv::Diagnostics::isSafetyEnabled()) {
         cv::detectMemoryLeaks();
     }
     return 0;
 }
diff --git a/engine/examples/backend_validation/main.cpp b/engine/examples/backend_validation/main.cpp
index 58d77b337d4380b719b923eac98876ab75216ad3..783c0dca71efd51abf0b922b9df032144d100527 100644
--- a/engine/examples/backend_validation/main.cpp
+++ b/engine/examples/backend_validation/main.cpp
@@ -1,23 +1,23 @@
 #include "core/engine/Engine.h"
-#include "modules/minimal/MinimalGame.h"
+#include "game/core/Game.h"
 #include "modules/debug/logging/logger.h"
 #include <cstring>
 #include <memory>
 
 int main(int argc, char** argv) {
     std::string displayConfig = "game/assets/config/backend_validation.json";
     for (int i = 1; i < argc; ++i) {
         if (std::strncmp(argv[i], "--display-config=", 17) == 0) {
             displayConfig = argv[i] + 17;
         }
     }
 
     cv::Engine engine;
-    auto game = std::make_unique<game::MinimalGame>();
+    auto game = std::make_unique<game::Game>();
     engine.Init(game.get(), "en", cv::BackendType::OpenGL,
-                displayConfig, false, false, false, 60.0f, true);
+                displayConfig, false, false, false, 60.0f);
     engine.Run();
     engine.Shutdown();
     return 0;
 }
 
diff --git a/game/core/flow/GameManager.cpp b/game/core/flow/GameManager.cpp
index e1ed81fcd33bcd7815527b37a6c1ec38e59160bf..d5d2e772fbd767e347b7e03024258addd67c4f31 100644
--- a/game/core/flow/GameManager.cpp
+++ b/game/core/flow/GameManager.cpp
@@ -1,101 +1,97 @@
 #include "GameManager.h"
 #include "modules/debug/logging/logger.h"
 #include "game/core/IGame.h"
 #include <string>
 #include "game/scene/Scene0.h"
 #include "game/scene/Scene1.h"
 #include "game/scene/Scene2.h"
 #include "game/scene/Scene3.h"
 #include "modules/system/powerup/PowerupManager.h"
 #include "engine/modules/network/manager/NetworkSimulationManager.h"
 #include "modules/hardware/hil/HILInterface.h"
 #include "modules/system/projectile/ProjectileManager.h"
 #include "game/modules/system/alignment/AlignmentManager.h"
 #include "game/modules/system/chapter/ChapterManager.h"
 
 namespace engine {
 
 GameManager::GameManager(game::IGame* game,
                          engine::InputManager* input,
                          cv::RenderSystem* render,
                          cv::SaveSystem* save,
-                         cv::LocalizationSystem* locSystem,
-                         bool minimalMode)
+                         cv::LocalizationSystem* locSystem)
     : m_prevState(GameState::None),
       m_game(game),
       m_input(input),
       m_render(render),
-      m_locSystem(locSystem),
-      m_minimalMode(minimalMode) {
+      m_locSystem(locSystem) {
     m_saveCoordinator.SetSaveSystem(save);
     m_saveCoordinator.SetGame(game);
 }
 
 GameManager::~GameManager() { m_IsAlive = false; }
 
 void GameManager::Update(float deltaTime) {
     m_sceneCoordinator.Update(deltaTime);
 }
 
 void GameManager::Render() {
     m_sceneCoordinator.Render();
 }
 
 void GameManager::SetState(GameState newState) {
     GameState current = m_phaseController.GetState();
     if (current == newState) {
         return;
     }
     m_prevState = current;
     m_phaseController.SetState(newState);
 
     switch (newState) {
     case GameState::MainMenu: {
         auto scene = std::make_unique<game::Scene0>(this, m_input, m_render);
         CV_LOG_INFO("GameManager: loading scene Scene0");
         m_sceneCoordinator.StartTransition(std::move(scene));
         m_sceneCoordinator.CompleteTransition();
         break;
     }
     case GameState::Hub: {
         auto scene = std::make_unique<game::Scene1>(this, m_input, m_render);
         CV_LOG_INFO("GameManager: loading scene Scene1");
         m_sceneCoordinator.StartTransition(std::move(scene));
         m_sceneCoordinator.CompleteTransition();
         break;
     }
     case GameState::Playing: {
         if (m_game) {
             m_game->StartGame();
         }
-        if (!m_minimalMode) {
-            auto scene = std::make_unique<game::Scene3>(this, m_input, m_render);
-            CV_LOG_INFO("GameManager: loading scene Scene3");
-            m_sceneCoordinator.StartTransition(std::move(scene));
-            m_sceneCoordinator.CompleteTransition();
-        }
+        auto scene = std::make_unique<game::Scene3>(this, m_input, m_render);
+        CV_LOG_INFO("GameManager: loading scene Scene3");
+        m_sceneCoordinator.StartTransition(std::move(scene));
+        m_sceneCoordinator.CompleteTransition();
         break;
     }
     case GameState::GameOver: {
         auto scene = std::make_unique<game::Scene2>(this, m_input, m_render);
         CV_LOG_INFO("GameManager: loading scene Scene2");
         m_sceneCoordinator.StartTransition(std::move(scene));
         m_sceneCoordinator.CompleteTransition();
         break;
     }
     default:
         break;
     }
 
     std::string stateName;
     switch (newState) {
     case GameState::MainMenu: stateName = "MainMenu"; break;
     case GameState::Hub: stateName = "Hub"; break;
     case GameState::Playing: stateName = "Playing"; break;
     case GameState::Paused: stateName = "Paused"; break;
     case GameState::GameOver: stateName = "GameOver"; break;
     default: stateName = "None"; break;
     }
     CV_LOG_INFO("GameManager: State changed to " + stateName);
 }
 
diff --git a/game/core/flow/GameManager.h b/game/core/flow/GameManager.h
index d4c78c7d1f6fdc1c55b3ea6fc855b30b864c6647..9a444b0fff970025b43aac780789043b46830088 100644
--- a/game/core/flow/GameManager.h
+++ b/game/core/flow/GameManager.h
@@ -5,72 +5,70 @@
 #include "PhaseController.h"
 #include "SaveLoadCoordinator.h"
 #include "modules/input/InputManager.h"
 #include "engine/modules/render/RenderSystem.h"
 #include "game/modules/system/save/SaveSystem.h"
 #include "game/modules/enemy/Enemy.h"
 #include "modules/system/projectile/ProjectileManager.h"
 #include "game/modules/system/powerup/Powerup.h"
 #include "modules/player/PlayerManager.h"
 #include "localization/system/LocalizationSystem.h"
 #include <string>
 
 namespace game {
 class IGame;
 }
 
 namespace engine {
 
 // GameManager orchestrates high-level game flow.
 class GameManager {
 public:
     GameManager(game::IGame* game,
                 engine::InputManager* input,
                 cv::RenderSystem* render,
                 cv::SaveSystem* save,
-                cv::LocalizationSystem* locSystem,
-                bool minimalMode = false);
+                cv::LocalizationSystem* locSystem);
     ~GameManager();
     bool isAlive() const { return m_IsAlive; }
 
     void Update(float deltaTime);
     void Render();
 
     // Change the current game state.
     void SetState(GameState newState);
 
     // Retrieve the current game state.
     GameState GetState() const;
     game::IGame* GetGame() const { return m_game; }
 
     SceneManager& GetSceneManager();
     GamePhaseManager& GetPhaseManager();
     cv::SaveSystem* GetSaveSystem();
     void SaveGame(const std::string& slot,
                   const std::string& profileName = "");
     bool LoadGame(const std::string& slot);
 
     bool HasPendingLoad() const;
     const cv::GameSaveData& GetPendingLoad() const;
     void ClearPendingLoad();
 
     void SetActiveProfile(const std::string& slot, const std::string& profile);
 
     const std::string& GetActiveSlot() const;
     const std::string& GetActiveProfile() const;
 
 private:
     SceneCoordinator m_sceneCoordinator;
     PhaseController m_phaseController;
     SaveLoadCoordinator m_saveCoordinator;
     GameState m_prevState;
     game::IGame* m_game = nullptr;
     engine::InputManager* m_input = nullptr;
     cv::RenderSystem* m_render = nullptr;
     cv::LocalizationSystem* m_locSystem = nullptr;
     bool m_IsAlive = true;
-    bool m_minimalMode = false;
 };
 
 } // namespace engine
 
 #endif // GAME_MANAGER_H
diff --git a/game/modules/minimal/MinimalGame.cpp b/game/modules/minimal/MinimalGame.cpp
deleted file mode 100644
index 7c54ec1a8b38ef546efa9750f820b95e68ca8784..0000000000000000000000000000000000000000
--- a/game/modules/minimal/MinimalGame.cpp
+++ /dev/null
@@ -1,25 +0,0 @@
-#include "MinimalGame.h"
-
-namespace game {
-
-void MinimalGame::Init([[maybe_unused]] engine::InputManager* inputManager,
-                       [[maybe_unused]] engine::SynthEngine* synthEngine,
-                       [[maybe_unused]] cv::RenderSystem* renderSystem,
-                       [[maybe_unused]] cv::ResourceSystem* resourceSystem,
-                       [[maybe_unused]] cv::DynamicsSystem* physicsSystem,
-                       [[maybe_unused]] cv::LocalizationSystem* localization,
-                       [[maybe_unused]] cv::ScriptingSystem* scripting,
-                       [[maybe_unused]] cv::SaveSystem* saveSystem,
-                       [[maybe_unused]] cv::TelemetryManager* telemetry,
-                       [[maybe_unused]] cv::HILInterface* hil,
-                       [[maybe_unused]] cv::NetworkSimulationManager* network,
-                       [[maybe_unused]] cv::ThemeManager* themeManager) {}
-
-void MinimalGame::Update([[maybe_unused]] float deltaTime) {}
-void MinimalGame::Render([[maybe_unused]] cv::RenderSystem* renderSystem) {}
-void MinimalGame::Shutdown() {}
-void MinimalGame::StartGame() {}
-void MinimalGame::EnableAutotest([[maybe_unused]] float duration) {}
-void MinimalGame::RegisterScriptBindings([[maybe_unused]] cv::ScriptingSystem& scripting) {}
-
-} // namespace game
diff --git a/game/modules/minimal/MinimalGame.h b/game/modules/minimal/MinimalGame.h
deleted file mode 100644
index 2d4cd69666892caa653c85221c2410c1770bbc8b..0000000000000000000000000000000000000000
--- a/game/modules/minimal/MinimalGame.h
+++ /dev/null
@@ -1,68 +0,0 @@
-#ifndef MINIMAL_GAME_H
-#define MINIMAL_GAME_H
-
-#include "core/IGame.h"
-#include "core/scene/Scene.h"
-#include "core/EntityManager.h"
-
-namespace cv { class ThemeManager; }
-
-namespace game {
-
-class MinimalGame : public IGame {
-public:
-    void Init([[maybe_unused]] engine::InputManager* inputManager,
-              [[maybe_unused]] engine::SynthEngine* synthEngine,
-              [[maybe_unused]] cv::RenderSystem* renderSystem,
-              [[maybe_unused]] cv::ResourceSystem* resourceSystem,
-              [[maybe_unused]] cv::DynamicsSystem* physicsSystem,
-              [[maybe_unused]] cv::LocalizationSystem* localization,
-              [[maybe_unused]] cv::ScriptingSystem* scripting,
-              [[maybe_unused]] cv::SaveSystem* saveSystem,
-              [[maybe_unused]] cv::TelemetryManager* telemetry,
-              [[maybe_unused]] cv::HILInterface* hil,
-              [[maybe_unused]] cv::NetworkSimulationManager* network,
-              [[maybe_unused]] cv::ThemeManager* themeManager) override;
-
-    void Update([[maybe_unused]] float deltaTime) override;
-    void Render([[maybe_unused]] cv::RenderSystem* renderSystem) override;
-    void Shutdown() override;
-    void StartGame() override;
-    void EnableAutotest([[maybe_unused]] float duration) override;
-    void RegisterScriptBindings([[maybe_unused]] cv::ScriptingSystem& scripting) override;
-
-    EntityManager& GetEntityManager() override { return m_entityManager; }
-    PlayerManager* GetPlayerManager() override { return nullptr; }
-    ProjectileManager* GetProjectileManager() override { return nullptr; }
-    EnemySpawner* GetEnemySpawner() override { return nullptr; }
-    EnemyManager* GetEnemyManager() override { return nullptr; }
-    CollisionSystem* GetCollisionSystem() override { return nullptr; }
-    GameStateManager* GetGameStateManager() override { return nullptr; }
-    PowerupManager* GetPowerupManager() override { return nullptr; }
-    ParticleManager* GetParticleManager() override { return nullptr; }
-    StageManager* GetStageManager() override { return nullptr; }
-    AlignmentManager* GetAlignmentManager() override { return nullptr; }
-    ChapterManager* GetChapterManager() override { return nullptr; }
-    AchievementManager* GetAchievementManager() override { return nullptr; }
-    FactionReputation* GetFactionManager() override { return nullptr; }
-    SpaceManager* GetSpaceManager() override { return nullptr; }
-    NarrativeEventSystem* GetNarrativeSystem() override { return nullptr; }
-    engine::QuantumStateVectorManager* GetQuantumStateVectorManager() override { return nullptr; }
-    engine::QuantumPatternTranslationLayer* GetQuantumPatternLayer() override { return nullptr; }
-    unsigned int GetEntropySeed() const override { return 0; }
-    cv::NetworkSimulationManager* GetNetworkManager() override { return nullptr; }
-    cv::HILInterface* GetHILInterface() override { return nullptr; }
-    cv::ProceduralSpriteManager* GetSpriteManager() override { return nullptr; }
-    cv::DynamicsSystem* GetPhysicsSystem() override { return nullptr; }
-    void TriggerQuest(const std::string& /*id*/) override {}
-    bool IsStarMapUnlocked() const override { return false; }
-    cv::Scene& GetScene() override { return m_scene; }
-
-private:
-    EntityManager m_entityManager;
-    cv::Scene m_scene;
-};
-
-} // namespace game
-
-#endif // MINIMAL_GAME_H
diff --git a/game/scene/DebugWindow0.cpp b/game/scene/DebugWindow0.cpp
index dac1778279eabd605f36932f8a7471b584858b26..e0eff2ec18b1b4f13e2a6a4673fa8e555af3f993 100644
--- a/game/scene/DebugWindow0.cpp
+++ b/game/scene/DebugWindow0.cpp
@@ -6,56 +6,53 @@
 #include <sstream>
 
 namespace game {
 
 void DebugWindow0::OnEnter() {
     CV_LOG_INFO(std::string("DebugWindow0::OnEnter index=") +
                 std::to_string(m_windowIndex));
     if (m_render) {
         m_render->setClearColor(1.0f, 1.0f, 0.0f, 1.0f, 1);
     }
 }
 
 void DebugWindow0::OnExit() {
     CV_LOG_INFO(std::string("DebugWindow0::OnExit index=") +
                 std::to_string(m_windowIndex) + " shutting down");
 }
 
 void DebugWindow0::Render() {
     CV_LOG_INFO(std::string("Rendering DebugWindow0 index=") +
                 std::to_string(m_windowIndex));
     if (!m_render) {
         return;
     }
     m_render->bindDefaultUiState();
     if (m_manager) {
-        if (auto* game = m_manager->GetGame()) {
-            if (auto* sm = game->GetSpriteManager()) {
-                sm->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
-                sm->drawAll();
-            }
-        }
+        auto* sm = m_manager->GetGame()->GetSpriteManager();
+        sm->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
+        sm->drawAll();
     }
 
     uint16_t vw = 0, vh = 0;
     m_render->getViewSize(vw, vh);
     float proj[16];
     m_render->getUiProjectionMatrix(proj);
     std::ostringstream mat;
     mat << proj[0];
     for (int i = 1; i < 16; ++i) mat << ',' << proj[i];
     CV_LOG_INFO(std::string("DebugWindow0 view=") + std::to_string(vw) + "x" +
                 std::to_string(vh) + " uiProj=" + mat.str());
 
     const int cols = 10;
     const int rows = 10;
     const float scale = 6.0f;
     const float spacing = 2.0f;
 
     const float charW = 4.0f * scale;
     const float charH = cv::VectorFont::lineHeight(scale);
 
     const float gridW = charW * cols + spacing * (cols - 1);
     const float gridH = charH * rows + spacing * (rows - 1);
 
     float startX = (static_cast<float>(vw) - gridW) * 0.5f;
     float startY = (static_cast<float>(vh) - gridH) * 0.5f;
diff --git a/game/scene/Scene0.cpp b/game/scene/Scene0.cpp
index 589a20d987b66b5467445368ea6a1d98d910311b..5292a6a9e8e3efcff14b43961795e400394e1694 100644
--- a/game/scene/Scene0.cpp
+++ b/game/scene/Scene0.cpp
@@ -40,56 +40,52 @@ void Scene0::OnEnter() {
 
     m_frameSprite.frames.clear();
     m_frameSprite.loop = false;
     auto drawFrame = [this](cv::RenderSystem& rs, const cv::SpriteParams&) {
         float w = 40.0f;
         float h = 10.0f;
         rs.drawLine(m_frameX - w * 0.5f, m_frameY - h * 0.5f,
                     m_frameX + w * 0.5f, m_frameY - h * 0.5f, 0xffffff00);
         rs.drawLine(m_frameX + w * 0.5f, m_frameY - h * 0.5f,
                     m_frameX + w * 0.5f, m_frameY + h * 0.5f, 0xffffff00);
         rs.drawLine(m_frameX + w * 0.5f, m_frameY + h * 0.5f,
                     m_frameX - w * 0.5f, m_frameY + h * 0.5f, 0xffffff00);
         rs.drawLine(m_frameX - w * 0.5f, m_frameY + h * 0.5f,
                     m_frameX - w * 0.5f, m_frameY - h * 0.5f, 0xffffff00);
     };
     m_frameSprite.frames.push_back({drawFrame, 0.1f});
     m_frameSprite.name = "Scene0Frame";
     m_frameInstance.sprite = &m_frameSprite;
     m_frameInstance.m_name = "Scene0Frame";
     m_frameInstance.m_source = "Scene0";
     m_frameInstance.lastRenderFrame = cv::getFrameCount();
     m_frameInstance.enableDrawLogs = true;
 
     cv::ProceduralSpriteManager* spriteMgr = nullptr;
     if (m_manager) {
-        if (auto* game = m_manager->GetGame()) {
-            spriteMgr = game->GetSpriteManager();
-            if (spriteMgr) {
-                spriteMgr->addSprite(&m_frameInstance);
-            }
-        }
+        spriteMgr = m_manager->GetGame()->GetSpriteManager();
+        spriteMgr->addSprite(&m_frameInstance);
     }
 
     // ------------------------------------------------------------------
     // Vector shape grid setup
     // ------------------------------------------------------------------
     const uint32_t black = 0xff000000u;
     const int cols = 4;
     const float spacing = unit * 5.0f;
     const float startX = m_frameX - spacing * (cols - 1) * 0.5f;
     const float startY = m_frameY + spacing;
 
     for (size_t i = 0; i < kShapeCount; ++i) {
         float px = startX + spacing * static_cast<float>(i % cols);
         float py = startY + spacing * static_cast<float>(i / cols);
         auto& sprite = m_shapeSprites[i];
         sprite.frames.clear();
         sprite.loop = false;
         switch (i) {
         case 0: // circle
             sprite.name = "Circle";
             sprite.frames.push_back({[px, py, black, unit](cv::RenderSystem& rs, const cv::SpriteParams&) {
                 rs.drawCircle(px, py, unit * 1.25f, black);
             }, 0.1f});
             break;
         case 1: // ellipse / egg
@@ -166,112 +162,99 @@ void Scene0::OnEnter() {
             break;
         case 10: // arrow
             sprite.name = "Arrow";
             sprite.frames.push_back({[px, py, black, unit](cv::RenderSystem& rs, const cv::SpriteParams&) {
                 rs.drawLine(px - unit * 1.25f, py, px + unit * 0.75f, py, black);
                 std::vector<cv::RenderSystem::Vec2> pts{{px + unit * 0.75f, py}, {px + unit * 0.375f, py - unit * 0.5f}, {px + unit * 0.375f, py + unit * 0.5f}};
                 rs.drawPolygon(pts, black);
             }, 0.1f});
             break;
         default: // composite
             sprite.name = "Composite";
             sprite.frames.push_back({[px, py, black, unit](cv::RenderSystem& rs, const cv::SpriteParams&) {
                 rs.drawCircle(px, py, unit * 0.75f, black);
                 rs.drawRect(px - unit * 0.75f, py - unit * 0.75f, unit * 1.5f, unit * 1.5f, black);
                 rs.drawLine(px - unit, py, px + unit, py, black);
                 rs.drawLine(px, py - unit, px, py + unit, black);
             }, 0.1f});
             break;
         }
         auto& inst = m_shapeInstances[i];
         inst.sprite = &sprite;
         inst.m_name = sprite.name;
         inst.m_source = "Scene0";
         inst.lastRenderFrame = cv::getFrameCount();
         inst.enableDrawLogs = true;
-        if (spriteMgr) {
-            spriteMgr->addSprite(&inst);
-        }
+        spriteMgr->addSprite(&inst);
     }
 }
 
 void Scene0::OnExit() {
-    cv::ProceduralSpriteManager* spriteMgr = nullptr;
     if (m_manager) {
-        if (auto* game = m_manager->GetGame()) {
-            spriteMgr = game->GetSpriteManager();
-            if (spriteMgr) {
-                spriteMgr->removeSprite(&m_frameInstance);
-                for (auto& inst : m_shapeInstances) {
-                    spriteMgr->removeSprite(&inst);
-                }
-            }
+        auto* spriteMgr = m_manager->GetGame()->GetSpriteManager();
+        spriteMgr->removeSprite(&m_frameInstance);
+        for (auto& inst : m_shapeInstances) {
+            spriteMgr->removeSprite(&inst);
         }
     }
 }
 
 void Scene0::Update(float deltaTime) {
     if (!m_pushed && m_input && m_manager) {
         if (m_input->IsKeyPressed(engine::KeyCode::Enter)) {
             auto next = std::make_unique<Scene1>(m_manager, m_input, m_render);
             auto& sm = m_manager->GetSceneManager();
             sm.StartTransition(std::move(next));
             sm.CompleteTransition();
             m_pushed = true;
         }
     }
     m_frameInstance.update(deltaTime);
     for (auto& inst : m_shapeInstances) {
         inst.update(deltaTime);
     }
 }
 
 void Scene0::Render() {
     CV_LOG_INFO("Rendering Scene0");
     if (!m_render) {
         return;
     }
     m_render->bindDefaultUiState();
     cv::ProceduralSpriteManager* spriteMgr = nullptr;
     if (m_manager) {
-        if (auto* game = m_manager->GetGame()) {
-            spriteMgr = game->GetSpriteManager();
-            if (spriteMgr) {
-                spriteMgr->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
-            }
-        }
+        spriteMgr = m_manager->GetGame()->GetSpriteManager();
+        spriteMgr->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
     }
     uint16_t vw = 0, vh = 0;
     m_render->getViewSize(vw, vh);
     float prev = m_render->getUIFontScale();
     float scale = 8.0f;
     m_render->setUIFontScale(scale);
     float vtw = 0.f, vth = 0.f;
     cv::VectorFont::measureTextBounds("A", scale, vtw, vth);
     float tx = static_cast<float>(vw) * 0.5f - vtw * 0.5f;
     float ty = static_cast<float>(vh) * 0.5f - vth * 0.5f;
 
     // Draw sprites first so VectorFont text appears on top
-    if (spriteMgr) {
-        spriteMgr->drawAll();
-    }
+    spriteMgr->drawAll();
 
     uint32_t vfColor = 0xff000000; // hardcoded black for VectorFont tests
     m_render->drawTextScreen("A", tx, ty, vfColor);
 
     // Render a vectorfont test string below the fallback letter
     float vecScale = 4.0f; // smaller scale for clarity
     float vecW = 0.f, vecH = 0.f;
     cv::VectorFont::measureTextBounds("VECTOR OK", vecScale, vecW, vecH);
     float vecX = tx;
     float vecY = ty + vth + 5.0f;
     m_render->bindDefaultUiState();
     m_render->logCurrentBindings();
     cv::VectorFont::drawTextScreen(*m_render, "VECTOR OK", vecX, vecY, vfColor, vecScale);
     GLenum err = glGetError();
     if (err != GL_NO_ERROR) {
         CV_LOG_ERROR(std::string("Scene0 VectorFont glGetError=") + std::to_string(err));
     }
     CV_LOG_INFO(std::string("Scene0 VectorFont context: ") + m_render->getLastDrawContext());
     m_render->setUIFontScale(prev);
 }
 } // namespace game
diff --git a/game/scene/Scene1.cpp b/game/scene/Scene1.cpp
index ed664bd8cbd58b3cf8af6c58abefb36ec94bf3e3..4bfb14e89868ad449238dc91ec7c50e7bccf8f98 100644
--- a/game/scene/Scene1.cpp
+++ b/game/scene/Scene1.cpp
@@ -17,45 +17,42 @@ void Scene1::OnEnter() {
     }
 }
 
 void Scene1::OnExit() {}
 
 void Scene1::Update(float deltaTime) {
     (void)deltaTime;
     if (!m_pushed && m_input && m_manager) {
         if (m_input->IsKeyPressed(engine::KeyCode::Enter)) {
             auto next = std::make_unique<Scene2>(m_manager, m_input, m_render);
             auto& sm = m_manager->GetSceneManager();
             sm.StartTransition(std::move(next));
             sm.CompleteTransition();
             m_pushed = true;
         }
     }
 }
 
 void Scene1::Render() {
     CV_LOG_INFO("Rendering Scene1");
     if (!m_render) {
         return;
     }
     m_render->bindDefaultUiState();
     if (m_manager) {
-        if (auto* game = m_manager->GetGame()) {
-            if (auto* sm = game->GetSpriteManager()) {
-                sm->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
-                sm->drawAll();
-            }
-        }
+        auto* sm = m_manager->GetGame()->GetSpriteManager();
+        sm->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
+        sm->drawAll();
     }
     uint16_t vw = 0, vh = 0;
     m_render->getViewSize(vw, vh);
     float prev = m_render->getUIFontScale();
     float scale = 8.0f;
     m_render->setUIFontScale(scale);
     float vtw = 0.f, vth = 0.f;
     cv::VectorFont::measureTextBounds("B", scale, vtw, vth);
     float tx = static_cast<float>(vw) * 0.5f - vtw * 0.5f;
     float ty = static_cast<float>(vh) * 0.5f - vth * 0.5f;
     m_render->drawTextScreen("B", tx, ty, 0xff000000);
     m_render->setUIFontScale(prev);
 }
 } // namespace game
diff --git a/game/scene/Scene2.cpp b/game/scene/Scene2.cpp
index fe83238147ebab0b7d236c3f5fcd6a1c43b09dd1..90f94011030cb8702a02a29b43e9da0cb3d98dd0 100644
--- a/game/scene/Scene2.cpp
+++ b/game/scene/Scene2.cpp
@@ -13,45 +13,42 @@ Scene2::Scene2(engine::GameManager* manager,
 
 void Scene2::OnEnter() {
     if (m_render) {
         m_render->setClearColor(0.0f, 0.0f, 1.0f, 1.0f);
     }
 }
 
 void Scene2::OnExit() {}
 
 void Scene2::Update(float deltaTime) {
     (void)deltaTime;
     if (m_input && m_manager) {
         if (m_input->IsKeyPressed(engine::KeyCode::Enter)) {
             m_manager->SetState(engine::GameState::Playing);
         }
     }
 }
 
 void Scene2::Render() {
     CV_LOG_INFO("Rendering Scene2");
     if (!m_render) {
         return;
     }
     m_render->bindDefaultUiState();
     if (m_manager) {
-        if (auto* game = m_manager->GetGame()) {
-            if (auto* sm = game->GetSpriteManager()) {
-                sm->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
-                sm->drawAll();
-            }
-        }
+        auto* sm = m_manager->GetGame()->GetSpriteManager();
+        sm->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
+        sm->drawAll();
     }
     uint16_t vw = 0, vh = 0;
     m_render->getViewSize(vw, vh);
     float prev = m_render->getUIFontScale();
     float scale = 8.0f;
     m_render->setUIFontScale(scale);
     float vtw = 0.f, vth = 0.f;
     cv::VectorFont::measureTextBounds("C", scale, vtw, vth);
     float tx = static_cast<float>(vw) * 0.5f - vtw * 0.5f;
     float ty = static_cast<float>(vh) * 0.5f - vth * 0.5f;
     m_render->drawTextScreen("C", tx, ty, 0xff000000);
     m_render->setUIFontScale(prev);
 }
 } // namespace game
diff --git a/game/scene/Scene3.cpp b/game/scene/Scene3.cpp
index ee545fa0b364faeaf123c3664020d21180087b94..e348fc816defc17135199c48037d1be0f1bb3465 100644
--- a/game/scene/Scene3.cpp
+++ b/game/scene/Scene3.cpp
@@ -5,45 +5,42 @@
 #include <algorithm>
 
 namespace game {
 Scene3::Scene3(engine::GameManager* manager,
                engine::InputManager* input,
                cv::RenderSystem* render)
     : m_manager(manager), m_input(input), m_render(render) {}
 
 void Scene3::OnEnter() {
     if (m_render) {
         m_render->setClearColor(1.0f, 1.0f, 0.0f, 1.0f);
     }
 }
 
 void Scene3::OnExit() {}
 
 void Scene3::Update(float deltaTime) { (void)deltaTime; }
 
 void Scene3::Render() {
     CV_LOG_INFO("Rendering Scene3");
     if (!m_render) {
         return;
     }
     m_render->bindDefaultUiState();
     if (m_manager) {
-        if (auto* game = m_manager->GetGame()) {
-            if (auto* sm = game->GetSpriteManager()) {
-                sm->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
-                sm->drawAll();
-            }
-        }
+        auto* sm = m_manager->GetGame()->GetSpriteManager();
+        sm->setRenderPass(cv::ProceduralSpriteManager::RenderPass::UI);
+        sm->drawAll();
     }
     uint16_t vw = 0, vh = 0;
     m_render->getViewSize(vw, vh);
     float prev = m_render->getUIFontScale();
     float scale = 8.0f;
     m_render->setUIFontScale(scale);
     float vtw = 0.f, vth = 0.f;
     cv::VectorFont::measureTextBounds("D", scale, vtw, vth);
     float tx = static_cast<float>(vw) * 0.5f - vtw * 0.5f;
     float ty = static_cast<float>(vh) * 0.5f - vth * 0.5f;
     m_render->drawTextScreen("D", tx, ty, 0xff000000);
     m_render->setUIFontScale(prev);
 }
 } // namespace game
