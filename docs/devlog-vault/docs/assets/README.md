# Assets Overview

This folder tracks external assets required by Purge of the Crescent Veil.

## Audio Files

Sound effects are synthesized in real time by `SynthEngine`.
The only `.ogg` files in this repository are looping music tracks.
If synthesis fails or the engine is not initialized, a warning will be logged
and playback skipped.
