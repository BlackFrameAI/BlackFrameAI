Launch (main.cpp)
  |
  |-- Parse command line options
  |-- SeedCore Startup
  |-- Create Game instance (Game.cpp)
  |-- Engine.Init(IGame*, language, backend, display config, etc.)
        |
        |-- Logger::initialize
        |-- MultiDisplayManager::initialize
        |-- InputBroker + InputManager
        |-- TimerManager / TimeSystem / FrameTimer / FrameLimiter
        |-- AudioBroker + SynthEngine
        |-- ResourceSystem (load shaders)
        |-- DynamicsSystem (PhysicsCore)
        |-- RenderSystem (OpenGL or other backend)
        |-- DebugOverlayWindow (optional)
        |-- PostProcessingManager + setupInvertPass
        |-- ScriptingSystem
        |-- SaveSystem
        |-- LocalizationSystem
        |-- ThemeManager
        |-- TelemetryManager
        |-- NetworkSimulationManager
        |-- EntropyManager (HardwareEntropySource, TimerEntropySource,
            InputTimingEntropySource, NetworkTimingEntropySource)
        |-- HILInterface
        |-- GameManager(game::IGame*, InputManager, RenderSystem, SaveSystem,
            LocalizationSystem)
        |-- Quantum systems initialized by Game (via Game::Init)
        |   - QuantumStateVectorManager
        |   - DualLayerQuantumManager (Hybrid + Oracle)
        |   - QuantumPatternTranslationLayer
        |-- Start initial scene:
            * GameManager.SetState(MainMenu)
  |
  |-- Engine.Run() main loop
        |
        |-- while IsRunning
              |-- InputManager::Update
              |-- MultiDisplayManager::pollEvents
              |-- FrameLimiter::beginFrame
              |-- FrameTimer::startFrame
              |-- SystemManager.updateSystems (Resource, Physics, Time)
              |-- NetworkSimulationManager::update
              |-- RenderSystem::beginFrame
              |-- Process input (ESC to exit, Enter to start game)
              |-- GameManager.Update (updates SceneCoordinator & PhaseController)
              |     |-- Active Scene Update (e.g. MinimalMainMenuScene,
              |         PlayState_Game)
              |     |-- Game.Update (Gameplay managers)
              |           |-- QuantumStateVectorManager::updatePerFrame
              |           |-- DualLayerQuantumManager::update
              |           |-- PlayerController / PlayerManager
              |           |-- EnemyManager / EnemySpawner
              |           |-- StageManager (waves, callbacks)
              |           |-- PowerupManager
              |           |-- CollisionSystem
              |           |-- BossManager
              |           |-- NarrativeEventSystem
              |           |-- SpaceManager
              |           |-- AchievementManager
              |           |-- AlignmentManager / ChapterManager
              |           |-- HubUpgradeManager / FactionReputation
              |           |-- ParticleManager / ProjectileManager
              |-- SynthEngine::Update
              |-- GameManager.Render
              |     |-- Active Scene Render
              |     |-- Game.Render (draw entities via RenderSystem)
              |-- TimeManager::Update
              |-- RenderSystem::endFrame
              |-- DebugOverlayWindow::render (optional)
              |-- MultiDisplayManager::presentAll
              |-- FrameTimer::endFrame
              |-- FrameLimiter::endFrame
              |-- incrementFrame (frame counter)
  |
  |-- Engine.Shutdown()
        |-- Cleanup Player/Game managers
        |-- Stop entropy thread EntropyManager::stop
        |-- Shutdown RenderSystem, DebugOverlayWindow, PostProcessingManager
        |-- Shutdown AudioBroker, SynthEngine
        |-- Shutdown InputManager, InputBroker
        |-- Shutdown all systems via SystemManager
        |-- Delete Game instance, Logger::shutdown
  |
  |-- SeedControl.ProcessRequests (handle kernel upgrade/rollback)
