# Game Menu System

The game menu system has been removed. Legacy modules `MainMenuScene` and
`HubMenuOverlay` no longer exist. Gameplay now transitions between placeholder
scenes `Scene0`, `Scene1` and `Scene2` for validation purposes. A fourth scene
`Scene3` is also available and currently loads after `Scene2` as the temporary
gameplay stub.
