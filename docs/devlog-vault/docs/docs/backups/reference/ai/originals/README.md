# Archived AI Reference Manifests

This folder stores the original manifest backups that contain the full external references used for internal tooling. Public versions have been sanitized for release and live under `docs/devlog-vault/public/docs/docs/reference/ai/`.
