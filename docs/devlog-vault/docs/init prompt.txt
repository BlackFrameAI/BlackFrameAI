🔰 PHASE INIT — ACTIVE CONTINUATION CONTEXT  
PROJECT: Purge of the Crescent Veil  
PHASE: 9.4 (Shader Fix → D3D11 Compliance + Codex Prompting Patch)  
GPT ROLE: Phase GPT (you), generating prompt logic for Codex in a brand new chat

---

✅ ENVIRONMENT OVERVIEW (LOCKED):
- User OS: **Windows 10+**
- GPU: **NVIDIA RTX 4090**
- Shader Backend: **BGFX**
- Shader Compiler: **shaderc.exe**, manually run in **PowerShell**
- Shader Platform Target: `--platform windows`
- Shader Profile: `--profile s_5_0`
- Shader includes resolved via: `engine/shaders/include/bgfx_shader.sh`

---

🧠 KEY CODING CONSTRAINTS (MUST FOLLOW):
- No `gl_Position`, `gl_FragColor`, or OpenGL-style variables
- Use `SV_POSITION` for vertex output
- Use `SV_TARGET0` for fragment output
- Use `out float4 ... : SV_TARGET0` HLSL semantics
- All includes must use: `#include "bgfx_shader.sh"`
- All shader source files reside in: `engine/shaders/`
- Varying file is: `engine/shaders/varying.def.sc`
- Include folder is: `engine/shaders/include/`

---

📂 CURRENT REPO STRUCTURE (CONFIRMED):
- `engine/shaders/vs_rect.sc`
- `engine/shaders/fs_rect.sc`
- `engine/shaders/varying.def.sc`
- `engine/shaders/include/bgfx_shader.sh` ← ✅ File is present and verified locally

---

🛠️ WHAT TO DO:
→ Use this context to build a Codex prompt that will:

🎯 Regenerate valid `.sc` shader files for D3D11 target

- `vs_rect.sc`: vertex shader, transforms position using `u_modelViewProj`, passes color to varying
- `fs_rect.sc`: fragment shader, receives color from varying and outputs it
- `varying.def.sc`: defines bridge between position/color attributes and varyings
- Assume compile will be done by user via:
```powershell
& $env:SHADERC -f engine/shaders/[file].sc -o engine/shaders/[file].bin --type [vertex|fragment] --platform windows --profile s_5_0 --varyingdef engine/shaders/varying.def.sc -i engine/shaders/include
