{
  "name": "blackframeai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "check:assets": "node scripts/check-assets.mjs"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
